import React from 'react';
import styles from './Home.module.scss';

interface Props {}

const Home = (props: Props) => {
  return (
    <div>
      <h1>Home</h1>
    </div>
  );
};

export default Home;
